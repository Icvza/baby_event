<% if @baby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baby.errors.count, "error") %> prohibited this article from being saved:</h2>  
      <ul>
        <% @baby.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>
<%= form_with model: @baby do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>
    <%= f.label :DOB %>
    <%= f.text_field :DOB %><br>
    <%= f.submit 'edit' %><br>
<% end %>

<%= link_to('delete', baby_path(@baby), method: 'delete') %>


