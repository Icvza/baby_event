<%= render partial: "profile" %>
<%= render partial: "userbabies" %>
 <%= form_with url: search_path, method: :get do |f| %>
  <%= f.label :content, "Search Events by content:" %>
  <%= f.text_field :content %>
  <%= f.submit "Search"%>
<% end %>

